/// <reference types="node" />
import { IdInfo, Options } from './types';
export declare class Koid {
    private readonly options;
    private readonly POW10;
    private readonly POW26;
    private readonly epoch;
    private readonly genId;
    private readonly seqMask;
    private lastTime;
    private seq;
    constructor(options: Options);
    /**
     * Generate an id, type of Buffer
     */
    get next(): Buffer;
    /**
     * Generate an id, type of bigint
     */
    get nextBigint(): bigint;
    get config(): Readonly<Options>;
    retrieveFromId(id: bigint | string | Readonly<Buffer>): IdInfo;
}
